This project uses a custom PHP contact form in `forms/contact.php`.

Configuration:
- SMTP settings are loaded from `forms/mailer-config.php`
- Example values are in `forms/mailer-config.example.php`
- Brevo setup notes are in `forms/BREVO-SETUP.md`
